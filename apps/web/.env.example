# Base URL for the todly API. Defaults to '/api/v1' (dev proxy forwards /api to :8080).
VITE_API_BASE_URL=/api/v1

# Web Push (PWA) public VAPID key — must match the server's PUSH_VAPID_PUBLIC.
# Leave blank to disable web push (native iOS/Android push is unaffected).
VITE_VAPID_PUBLIC_KEY=
